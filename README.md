# ai-beat-engine
markdown
# 🎵 AI Beat Maker - Deep'in Müzik Devrimi!

## 🚀 Hızlı Başlangıç
bash
# Backend
cd backend && pip install -r requirements.txt && python main.py

# Frontend (yeni terminal)  
cd frontend && npm install && npm run dev
